// Todo category select
const todoCatSelect = document.querySelector("select");

// Todo category h2
const h2 = document.querySelector("h2");

// Add todo form
const addTodoForm = document.querySelector("form");

// Get all inputs
const inputs = document.querySelectorAll("input");
// Add category input
const categoryInput = inputs[0];
// Add todo input
const addTodoInput = inputs[1];

// Get all buttons
const buttons = document.querySelectorAll("button");
const addCategoryButton = buttons[0];
// Add todo button
const addButton = buttons[1];

// Get todos section
const section = document.querySelector("section");
